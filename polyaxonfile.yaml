version: 1

kind: experiment

build:
  image: gw000/keras
  build_steps:
    - pip install polyaxon-client
    - pip install pandas
    - pip install scikit-learn
    - pip install keras
    - pip install tensorflow
    - pip install gensim
    - pip install numpy
    - pip install joblib
    - pip install paramiko
    
run:
  cmd: python3 createVector.py